#include<iostream>
using namespace std;
int main()
{
	/*cin.get().get();相当于cin.get(); 
	                        cin.get();*/
	/*C语言中，如果函数接受char指针和int参数，则使用该函数时，不能只传递一个参数，但在C++中
	可以这样做，因为该语言支持函数重载的OOP特性。函数重载允许创建多个同名函数，条件是他们的
	的参数列表不同。例如：如果使用cin.get(name,arsize)，则编译器将找到使用char*和int作为
	参数的cin.get()版本；使用cin.get(ch)也是一样的道理*/
	return 0;
}